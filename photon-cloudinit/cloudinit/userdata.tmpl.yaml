#cloud-config
users:
  - default
  - name: root
    lock-passwd: false
    passwd: ${root_password}
  - name: ${user_name}
    ssh-authorized-keys:
      - ${photon_ssh_pub_key}
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    groups: sudo
    shell: /bin/bash
packages: ${encoded_packages}
${additional}

